<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>OK-Kyle</title>
    <%= favicon_link_tag 'hat.png' %>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <% if current_user %>
      <div class="row">
        <div class="columns small-2 center">
          <img src=<%=current_user.image %>/>
        </div>
        <div class="nav columns small-2 center">
          <%=current_user.name %>
        </div>
        <div class="nav columns small-2 center">
          <%=link_to "Profile", users_path %>
        </div>
        <div class="nav columns small-2 center">
          <%=link_to "Find my Kyle", ok_kyle_path %>
        </div>
        <div class="nav columns small-2 center">
          <%=link_to "Sign Out", signout_path %>
        </div>
      </div>
    <% end %>
    <%= flash[:notice] %>

    <%= yield %>

  </body>
</html>
